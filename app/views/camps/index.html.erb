<div class="container">
  <div class="page-header">
    <div class="row">
      <div class="span9">
        <h1>Camps for <%= link_to "#{@camps.first.company.name} Company", company_path(@camps.first.company) %></h1>
      </div>
      <div class="span3">
        <%= link_to "Add new one", new_camp_path, :class => 'btn btn-success' %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="span9">
      <%= render :partial => 'table' %>
    </div>
  </div>
</div>

