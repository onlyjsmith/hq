<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Details (edit/delete)</th>
    </tr>
  </thead>
  <tbody>
    <% @camps.each do |camp| %>
    <tr>
      <td>
        <%= link_to camp.name, camp_path(camp) %>
      </td>
      <td>
        <%= link_to "Details", edit_camp_path(camp), :class => "btn btn-mini btn-warning" %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
